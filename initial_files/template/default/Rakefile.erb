<%= component('rakefile_common.erb') %>

LaTeXProjectTemplate::Task.new(MAIN_TEX_FILE) do |task|
  # * Set the path of latexmk
  # task.latexmk.path = '/usr/bin/latexmk'
  # 
  # * Add patterns to clean temporary files
  # task.clean.include("#*#")
  # 
  # * Default task to compile tex file. You can set :dvi, :ps, :pdf, :pdfdvi, or :pdfps.
  #   The default is :pdf.
  # task.default = :dvi
end
